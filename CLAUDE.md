# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 現在の状態

`develop` ブランチの最新コミットで全ファイルが削除されており、作業ツリーは空です。プロジェクトはこれから構築される予定です。

以前のコミット（`main` ブランチや `develop` の `0799e75` 時点）には React + TypeScript のコード（react-query, zustand, react-router, axios を使用した料理・材料管理アプリ）が存在していましたが、現時点ではこれを前提にせず、実際にリポジトリ内に存在するファイルを確認した上で作業してください。

新しい構成やコマンドが決まり次第、このファイルを更新してください。
